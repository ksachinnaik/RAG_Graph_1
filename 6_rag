# ============================================
# Cell 6A : Debug Extracted Links
# ============================================

print("=" * 80)
print("LINK EXTRACTION DEBUG")
print("=" * 80)

total_links = 0

for doc_id, links in all_links.items():

    row = df_clean.filter(df_clean.doc_id == doc_id).first()

    print(f"\nDoc ID      : {doc_id}")
    print(f"Reference   : {row['reference']}")
    print(f"Total Links : {len(links)}")

    total_links += len(links)

    if len(links) == 0:
        print("   No links found")
    else:
        for relation, ref in links:
            print(f"   {relation:<15} --> {ref}")

print("\n" + "=" * 80)
print(f"Total Documents : {len(all_links)}")
print(f"Total Links     : {total_links}")
print("=" * 80)
