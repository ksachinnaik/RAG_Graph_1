# ============================
# Cell 6: Graph Construction (FIXED)
# ============================

import networkx as nx

# ✅ Use Directed Graph (IMPORTANT)
G = nx.DiGraph()

# Step 1: Add ALL nodes (parent + child)
for node in all_nodes:
    G.add_node(node)

# Step 2: Add edges from triples
for s, p, o in triples:
    G.add_edge(s, o, relation=p)

# Step 3: Print stats
print("Nodes:", len(G.nodes()))
print("Edges:", len(G.edges()))

print("\nSample Edges:")
for edge in list(G.edges())[:10]:
    print(edge)
